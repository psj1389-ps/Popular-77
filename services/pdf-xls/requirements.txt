# --- 기본 웹 서버 ---
Flask>=3.0.0
gunicorn>=21.2.0
flask-cors>=4.0.0
python-dotenv==1.0.0
requests>=2.31.0

# --- Adobe SDK (편집 가능한 변환) ---
pdfservices-sdk==4.2.0

# --- 폴백(Fallback)용: 이미지 기반 변환 ---
Pillow>=10.2.0
PyMuPDF>=1.26.4,<1.27
openpyxl>=3.1.5